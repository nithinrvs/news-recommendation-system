import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // You can remove this line if there's no CSS file
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
